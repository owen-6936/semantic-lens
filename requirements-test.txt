# Test deps for CI — excludes torch/easyocr (mocked in tests/conftest.py).
# Install with: pip install -r requirements-test.txt

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
python-multipart>=0.0.9
aiofiles>=23.0.0
pyyaml>=6.0.0
Pillow>=10.0.0
numpy>=1.26.0

# Test runner
pytest>=8.0.0
httpx>=0.27.0          # required by FastAPI TestClient
